spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/activityservice
      database: activityservice

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
